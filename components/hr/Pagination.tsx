import Link from "next/link";
import { ChevronLeft, ChevronRight } from "lucide-react";
import { Button } from "@/components/ui/button";
export default function Pagination({ page, totalPages, parameters }: { page: number; totalPages: number; parameters: Record<string, string | undefined> }) { const href = (nextPage: number) => { const search = new URLSearchParams(Object.entries({ ...parameters, page: String(nextPage) }).filter(([, value]) => value)); return `?${search}`; }; if (totalPages < 2) return null; return <div className="flex items-center justify-between border-t px-4 py-3"><p className="text-sm text-muted-foreground">Page {page} of {totalPages}</p><div className="flex gap-2"><Button variant="outline" size="sm" disabled={page === 1} asChild={page > 1}>{page > 1 ? <Link href={href(page - 1)}><ChevronLeft /> Previous</Link> : <span><ChevronLeft /> Previous</span>}</Button><Button variant="outline" size="sm" disabled={page === totalPages} asChild={page < totalPages}>{page < totalPages ? <Link href={href(page + 1)}>Next <ChevronRight /></Link> : <span>Next <ChevronRight /></span>}</Button></div></div>; }
